{
  "version": 2,
  "builds": [
    {
      "src": "api/app.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["index.html"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/app" }
  ]
}
