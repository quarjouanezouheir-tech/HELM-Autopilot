{
  "version": 2,
  "builds": [
    {
      "src": "api/app.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["index.html"],
        "runtime": "nodejs20.x"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/app" }
  ]
}
